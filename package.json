{
  "name": "kabinet",
  "version": "0.0.1",
  "description": "A light-weight social media",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "google-trends-api": "^4.9.2"
  },
  "scripts": {
    "start": "nodemon backend",
    "build": "cd frontend && npm install && npm run build",
    "dev": "concurrently --kill-others-on-fail \"npm run start\" \"cd frontend && npm run start\""
  },
  "cacheDirectories": [
    "node_modules",
    "frontend/node_modules"
  ],
  "devDependencies": {
    "concurrently": "^6.2.0",
    "nodemon": "^2.0.7"
  }
}
